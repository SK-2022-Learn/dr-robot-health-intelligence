# Dr. Robot Web

Next.js App Router frontend for the Dr. Robot prototype.

## Development

From this directory, install dependencies and start the local server:

```powershell
npm install
npm run dev
```

The frontend expects the API at `http://localhost:8000` unless
`NEXT_PUBLIC_API_BASE_URL` is set in `.env.local`.

